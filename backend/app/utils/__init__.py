# sales_agentic_ai/app/utils/__init__.py
"""Utility package — small, stateless helper modules used across the backend.

Modules:
  json_parse    — robust JSON extraction from raw LLM responses.
  similarity    — pure-Python cosine similarity and top-k ranking.
  text_cleaning — HTML entity decoding, whitespace normalisation, deduplication.
"""
